/**
 * WorkspaceClient — SUPERSEDED in Phase 2.
 *
 * This component is no longer used. The Phase 2 architecture splits the
 * workspace into two purpose-built components:
 *
 *   - WorkspaceHome      — surah grid (workspace home page)
 *   - WorkspacePageView  — full shell (Rail + Sidebar + TopBar + content)
 *
 * This file is kept for reference only and will be removed in Phase 3
 * when the editor and canvas are wired up to WorkspacePageView.
 *
 * Do not import this component.
 */

export default function WorkspaceClient() {
  return null;
}
